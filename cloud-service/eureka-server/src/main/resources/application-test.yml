server:
  port: 9003

spring:
  application:
    name: eureka-server
  boot:
    admin:
      url: http://192.168.3.100:9002

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone:  http://192.168.3.100:${server.port}/eureka/
  server:
    enable-self-preservation: false

management:
  security:
    enabled: false